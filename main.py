# Import the harmony module, which contains the Discord bot implementation
import harmony

# Check if the script is being run directly (not imported as a module)
if __name__ == "__main__":
    # Run the Discord bot by calling the run_bot function from the harmony module
    harmony.run_bot()